import { DiagramTool } from './modules/diagramTool.js';

// Initialize the diagram tool when the DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    const diagramTool = new DiagramTool();
});